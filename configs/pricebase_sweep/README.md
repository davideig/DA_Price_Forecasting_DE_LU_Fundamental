# RQ2 Price Configs

This folder now contains only the controlled RQ2 price-model configs used in
the thesis result tables.

The old cluster/window/hyperparameter sweeps were moved to:

```text
configs/archive/legacy_pricebase_dev_20260920/
```

Use `configs/FINAL_MODELS.md` for the current run order.
